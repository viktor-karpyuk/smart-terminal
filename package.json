{
  "name": "smart-terminal",
  "productName": "Smart Terminal",
  "version": "0.2.0-beta.2",
  "description": "A VS Code-style workbench for running many Claude Code sessions at once, each on a different Claude account.",
  "main": "electron/main.js",
  "author": "Viktor Karpyuk",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n vite,electron -c cyan,magenta \"vite\" \"wait-on tcp:5173 && cross-env SMART_TERMINAL_DEV=1 electron .\"",
    "build": "vite build",
    "start": "npm run build && electron .",
    "rebuild": "electron-rebuild -f -w node-pty",
    "postinstall": "electron-rebuild -f -w node-pty",
    "dist": "npm run stamp && npm run build && electron-builder --mac --config electron-builder.config.js",
    "test": "tsc src/state/layout.ts --outDir .test-build --module commonjs --target es2022 --moduleResolution node --skipLibCheck && node test/layout.test.js && node --test test/transcript.test.js test/turn-state.test.js test/autopilot.test.js test/restore.test.js test/cli-env.test.js",
    "typecheck": "tsc --noEmit",
    "stamp": "node scripts/stamp-build.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-search": "^0.16.0",
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.1.0",
    "concurrently": "^9.1.2",
    "cross-env": "^10.0.0",
    "electron": "^44.0.0",
    "electron-builder": "^26.15.3",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "typescript": "^5.9.0",
    "vite": "^8.2.2",
    "wait-on": "^9.0.0",
    "zustand": "^5.0.15"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/viktor-karpyuk/smart-terminal.git"
  }
}
