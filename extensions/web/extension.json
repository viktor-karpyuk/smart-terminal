{
  "id": "web",
  "name": "HTML and SVG",
  "version": "1.0.0",
  "author": "Smart Terminal",
  "summary": "See a page as a page, and a drawing as a drawing.",
  "description": "Renders .html untouched in a frame with no scripts and no origin, and opens .svg as the picture it is. A page's own scripts do not run and it cannot read files from beside it — which is the point, not a shortcoming.",
  "contributes": {
    "previews": [
      {
        "kind": "html",
        "extensions": [
          "html",
          "htm",
          "xhtml"
        ]
      },
      {
        "kind": "svg",
        "extensions": [
          "svg"
        ]
      }
    ]
  }
}
