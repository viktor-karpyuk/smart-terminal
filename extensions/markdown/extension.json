{
  "id": "markdown",
  "name": "Markdown",
  "version": "1.0.0",
  "author": "Smart Terminal",
  "summary": "Read Markdown as it is meant to be read.",
  "description": "Renders .md as a document — headings, tables, fenced code, task lists — beside or instead of the source. The preview follows the buffer, so it keeps up with what you type.",
  "contributes": {
    "previews": [
      {
        "kind": "markdown",
        "extensions": [
          "md",
          "markdown",
          "mdown",
          "mkd"
        ]
      }
    ]
  }
}
